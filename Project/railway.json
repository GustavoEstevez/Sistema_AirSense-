{
  "deploy": {
    "startCommand": "bash -c 'cd /app && python manage.py collectstatic --noinput && gunicorn --pythonpath /app Project.wsgi:application --bind 0.0.0.0:$PORT --workers 1 --threads 8 --timeout 60'"
  }
}